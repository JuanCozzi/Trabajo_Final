./env/Scripts/activate

$env:DJANGO_SETTINGS_MODULE = 'webserver.settings'


# The “deactivate” command is not a binary, nor a script that you “source”;
# it is a shell alias that gets defined dynamically in your current shell by the “activate” script
